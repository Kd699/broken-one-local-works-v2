{
  "name": "time-distribution-tool",
  "version": "1.0.0",
  "description": "Time Distribution Tool with Claude Integration",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "deploy": "firebase deploy --only hosting --non-interactive",
    "deploy-all": "firebase deploy --non-interactive",
    "build": "echo 'Build complete - static files ready'",
    "predeploy": "npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "node-fetch": "^2.7.0"
  }
}
